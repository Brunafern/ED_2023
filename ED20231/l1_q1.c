/* #include <stdio.h>

int main(void) {
  float fixo, vendas;
  char la[100];
  scanf("%s", la);
  scanf("%f""%f", &fixo, &vendas);
  float a = vendas * 0.15;
  printf("TOTAL = %.2f", fixo + a);
  return 0;
}*/