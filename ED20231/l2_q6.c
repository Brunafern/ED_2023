/*
#include <stdio.h>
# include <math.h>

int main(void) {
  int a, b ,c;
  double delta, x1, x2;
  scanf("%d""%d""%d", &a, &b,&c);
  delta= (b*b)-(4*a*c);
  if (delta<0){
    printf("Raizes imaginarias");
  } else if (delta == 0){
    x2 = (-b)/(2*a);
      x1 = x2;
    printf("%f""%f", x1, x2);
  } else {
    x1 = (-b + (sqrt(delta)))/(2*a);
    x2 = (-b - (sqrt(delta)))/(2*a);
    printf(" x1 =  %.2f \n"" x2 =  %.2f \n", x1, x2);
  }
  
  }
*/