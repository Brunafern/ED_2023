/* #include <stdio.h>
#include <stdlib.h>

int main(void) {
  float  vetor[30];
  int maior = 0, menor = 1111;
  
  for(int i = 0; i<30; i++){
    
    vetor[i] = rand()%1000;

    if (maior < vetor[i]){
      maior = vetor[i];
    } if (menor > vetor[i]){
      menor = vetor[i];
    }
    
    
    
    
    }
  printf("maior: %d menor: %d", maior, menor);
  }
  
  
*/